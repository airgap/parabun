// DEPRECATED: forwards to `@para/arena`. Import from that package
// directly when possible. Kept so existing `parabun-browser-shims/arena`
// consumers don't break during the per-module split — slated for
// removal in 0.4.x.

export * from "@para/arena";
export { default } from "@para/arena";
