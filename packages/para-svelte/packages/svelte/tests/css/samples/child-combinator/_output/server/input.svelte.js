import * as $ from 'svelte/internal/server';

export default function Input($$renderer) {
	$$renderer.push(`<main class="svelte-xyz"><div class="svelte-xyz"><button type="submit" class="svelte-xyz">Blue</button></div></main>`);
}