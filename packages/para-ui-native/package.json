{
  "name": "@lyku/para-ui-native",
  "version": "0.0.1-pre.0",
  "description": "Native-reactive .pui components + handle adapters — bind parabun's native capability modules (audio/camera/llm/gpu) into component-reactive `source` cells. The Para UI moat: reactive state that IS a native capability. Pre-release.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./AudioMeter.pui": "./src/AudioMeter.pui"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@lyku/para-signals": "^0.0.1-pre.1"
  }
}
