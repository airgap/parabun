// DEPRECATED: forwards to `@para/signals`. Import from that package
// directly when possible. Kept so existing
// `parabun-browser-shims/signals` consumers don't break during the
// per-module split — slated for removal in 0.4.x.

export * from "@para/signals";
export { default } from "@para/signals";
