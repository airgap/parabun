// DEPRECATED: forwards to `@para/parallel`. Import from that package
// directly when possible. Kept so existing
// `parabun-browser-shims/parallel` consumers don't break during the
// per-module split — slated for removal in 0.4.x.

export * from "@para/parallel";
export { default } from "@para/parallel";
